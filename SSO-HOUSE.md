# บ้าน `pnd51` — การเชื่อม SSO บ้านกลาง (ตามมาตรฐานรวมบ้าน ก้อน C)

**สถานะ:** สร้างฝั่งบ้านเสร็จ ผ่านเทสกับ mock บ้านกลางครบเกณฑ์ตรวจรับ — **รอของจาก admin บ้านกลาง (หัวข้อ 5) แล้วขึ้น Preview ได้ทันที**
**อ้างอิง:** `bbt-leadsheet/SSO/sso-house-guide-C-spec.md` (r2) · `BBT-แจ้งทีม-SSO-ก้อนB-14สค2026.md` — โค้ดฝั่งบ้านลอกโครงจาก `bbt-leadsheet/webapp/main.py` ซึ่งผ่าน grill แล้ว
**slug ที่เสนอ:** `pnd51` (ตั้งผ่าน env `HOUSE_SLUG` ได้)

---

## 1. สถาปัตยกรรม — แอปเดียว 2 โหมด

| โหมด | เปิดยังไง | ข้อมูลอยู่ไหน | การเข้าถึง |
|---|---|---|---|
| ออฟไลน์ (เดิม) | เปิดไฟล์ `index.html` ตรง ๆ (file://) | localStorage + IndexedDB ในเครื่อง | ไม่มี login (Firebase sync เดิมยังอยู่ครบ — ใช้ได้จนกว่า SSO ขึ้นจริง) |
| **บ้าน (webapp/)** | เสิร์ฟผ่าน FastAPI (`webapp/main.py`) | ฐานข้อมูลบ้าน (SQLite/Postgres) ผ่าน REST | **ประตูบ้านกลางทางเดียว** (SSO ก้อน C) · ก่อน SSO ขึ้นใช้ `ACCESS_CODE` ชั่วคราวได้ |

- frontend ตัวเดียวกัน — backend ฉีด `window.__BBT_REMOTE`/`__BBT_SSO` ตอนเสิร์ฟ
- โหมดบ้าน: **Firebase ถูกปิดทั้งชุด** (ไม่โหลดสคริปต์เลย — C9 ช่องเข้ามีทางเดียว) · localStorage ไม่เก็บข้อมูลลูกค้า (เครื่องสำนักงานใช้ร่วมกัน) · ข้อมูลลูกค้าเก่าที่ค้างจากโหมดออฟไลน์ถูกล้างเมื่อเข้าโหมด SSO (ล้อมติ Meth 25 ส.ค. ของ Lead Sheet)
- ไม่มี realtime/heartbeat — ดึงข้อมูลตอนเปิดหน้า/ปลดล็อกจอ/กดปุ่ม "🔄 ซิงค์เดี๋ยวนี้" เท่านั้น (มติ r6: session ค้างต้องไม่สร้าง traffic)
- เอกสารแนบ (ใบตอบรับ/ใบเสร็จ/งบทดลอง) ยังเก็บใน IndexedDB ของเครื่องตามดีไซน์เดิม — **ไม่ขึ้นเซิร์ฟเวอร์** ⚠️ ถ้าจะย้ายขึ้น server ในอนาคตต้องเดินตาม C8 (private bucket + signed URL)

## 2. ตาราง map role (ข้อเสนอ — ล้อมติ Lead Sheet 15 ส.ค. · **รอ Meth เคาะ**)

| role บ้านกลาง | ความหมายใน ภ.ง.ด.51 |
|---|---|
| `viewer` | ดูอย่างเดียว — server ปฏิเสธการเขียนทุกอย่าง |
| `editor` | จัดทำ + ตรวจ: ส่งตรวจ · ผ่านการตรวจ · ส่งอนุมัติ · ตีกลับ (ชั้นผู้ตรวจ) · บันทึกยื่นแบบ/ชำระเงิน · นำเข้า-ส่งออก (ล้อมติ v3.44: การรีวิวเป็นคุณสมบัติต่องาน ไม่ใช่ตำแหน่ง) |
| `full` | ทุกอย่างของ editor + **อนุมัติ · ตีกลับหลังส่งอนุมัติ · เปิดแก้ไขใหม่ · ลบบริษัท** — ทั้งหมดเป็น "การกระทำระดับ full" ต้อง **re-SSO ผ่านประตูใหม่ก่อนทุกครั้ง** (C4) |

การบังคับทำ 2 ชั้น: UI ซ่อน/บล็อกปุ่ม + **server ตรวจ transition ของ `workflow.status` เอง** (`reviewed→approved`, `reviewed→draft`, `approved/filed/completed→draft` และการแก้ลายเซ็นผู้อนุมัติ = full + re-SSO) — browser ยัดตรงก็ไม่ผ่าน
**ใครกด คนนั้นเซ็น:** ตอนเปลี่ยนสถานะ server ประทับชื่อ+รหัสผู้ใช้จาก session ทับค่าที่ browser ส่งมา

## 3. ข้อผูกพัน C1-C10 — จุดที่ implement

| ข้อ | ที่อยู่ในโค้ด |
|---|---|
| C1 รับตั๋ว + strip ทันที | `index.html` สคริปต์แรกสุดใน `<head>` · ส่งต่อทาง POST body เท่านั้น |
| C2 แลกบัตร + กติกาเหล็กห้าม retry | `webapp/main.py` `POST /api/sso/login` (ไม่เก็บตั๋ว จบในคำขอเดียว) · ฝั่งหน้าเว็บนับ 401 ติดกัน ≥2 = หยุดวนเตือนเช็คกุญแจ |
| C3 บัตรอยู่ server เท่านั้น · session ≤12 ชม. ไม่ต่ออายุ | ตาราง `sessions` (cookie เก็บแค่ token ที่ hash แล้ว, httpOnly) · `_session_public` ไม่มี badge |
| C4 ตรวจสด ≤15 นาที · ล็อกจอ · re-SSO ระดับ full | middleware `_ensure_fresh` + header `X-SSO-Checked-At` · `#ssoLock` จับเวลาในเครื่อง (ไม่มี heartbeat) · `_sso_fresh_for_full` |
| C5 บ้านกลางขัดข้อง | `_central_post` → 503 `central_down`: ไม่เปิดข้อมูลใหม่/ไม่รับเขียน หน้าจอเดิมดูต่อได้ · log เตือนเมื่อเงียบเกิน 30 นาที (คนแจ้ง Discord เอง) |
| C6 แปลง role · ห้าม cache | ตาราง map ข้อ 2 · สิทธิ์อ่านจากผลตรวจสดเท่านั้น · ตาราง `users` เป็น mapping/cache แสดงผล |
| C7 กุญแจใน env เท่านั้น | `HOUSE_KEY` — ไม่มีในโค้ด/หน้าเว็บ/log · dev กับ production คนละดอก |
| C8 ไฟล์ลูกค้า | ไม่มีไฟล์ลูกค้าบน server (แนบอยู่ IndexedDB เครื่องผู้ใช้) · ข้อมูลลูกค้าห้าม commit ตาม .gitignore เดิม |
| C9 ปิดช่อง login อื่น | SSO เปิด → `ACCESS_CODE` ปิดอัตโนมัติ · โหมดบ้านไม่โหลด Firebase auth เลย (Google/อีเมล+รหัสผ่านเหลือเฉพาะโหมดออฟไลน์เดิม) |
| C10 ลิงก์กลับบ้านกลาง | ปุ่ม "⌂ กลับบ้านกลาง" → `{BBT_URL}/switch` ที่แถบบน + การ์ดระบบกลาง + จอล็อก + จอทางตัน 403 |

## 4. เทส (mock บ้านกลางเท่านั้น — ห้ามยิงเคสผิดปกติใส่ production จริง)

```
pip install -r webapp/requirements.txt
node tests/sso.test.mjs        # หรือรวมทุกชุด: node tests/run-all.mjs
```
ครอบเกณฑ์ตรวจรับ 8 ข้อ (28 checks): ผ่านประตู→ไม่มี `#ticket=` ค้าง · ตั๋วซ้ำ→401 ไม่ retry + เหตุร้ายแรงฝั่งกลาง · ถูกปิดสิทธิ์→หลุดเมื่อผลตรวจหมดอายุ · นั่งเฉย→จอล็อก + **ศูนย์ traffic ระหว่างเฉย** · อนุมัติ→ถูกบังคับ re-SSO (ทั้ง UI และ server) · บ้านกลางล่ม→อ่าน/เขียนใหม่ถูกบล็อก จอเดิมดูได้ · บัตร/กุญแจไม่โผล่ browser · ACCESS_CODE ปิดเมื่อ SSO เปิด
รันจริงบนเครื่องเทส: `cd webapp && uvicorn mock_central:app --port 8761` + `SSO_ENABLED=1 BBT_URL=http://localhost:8761 HOUSE_KEY=devkey-pnd51 uvicorn main:app --port 8760` (mock ตั้ง `MOCK_HOUSE_URL=http://localhost:8760`)

## 5. ของที่ต้องได้จาก admin บ้านกลาง (Meth) ก่อนขึ้นจริง

1. **เคาะตาราง map role** (ข้อ 2) — โดยเฉพาะ: ตีกลับหลังส่งอนุมัติ/เปิดแก้ไขใหม่ ควรอยู่ระดับ full ตามที่เสนอหรือผ่อนเป็น editor
2. **ลงทะเบียนบ้าน `pnd51`** ในทะเบียนบ้าน + ระบบสิทธิ์ของบ้านกลาง (แบบเดียวกับ `audit_work`/`ocr`)
3. **กุญแจเข้าบ้าน (entry)** — dev (จากหน้า "ทะเบียนกุญแจ" บน staging) 1 ดอก + production 1 ดอก · **รับต่อหน้าเท่านั้น ห้ามแชต/LINE** (มติ 13 ส.ค.)
4. **`BBT_URL`** ของ Preview และ Production
5. ฝั่งเราส่งกลับ: **URL ของบ้านนี้** (Render · production ต้อง `https://`) ให้บ้านกลางตั้งปลายทาง redirect
6. เทสจริงกับ **บ้านกลางฝั่ง Preview เท่านั้น** ตามเกณฑ์หัวข้อ 5 ของ spec ก้อน C

## 6. Deploy (Render)

`render.yaml` อยู่ root ของ repo — New + → Blueprint → ชี้ repo นี้ · env ที่ต้องตั้งใน Dashboard: `DATABASE_URL` (แนะนำ Neon Postgres) · ช่วงก่อน SSO: `ACCESS_CODE` · เมื่อได้ของครบ: `SSO_ENABLED=1` + `BBT_URL` + `HOUSE_KEY` (แล้ว ACCESS_CODE ถูกปิดอัตโนมัติ)
เมื่อ SSO ขึ้นจริง: ปิดช่องทางเดิมทั้งหมดตาม C9 — โหมดออฟไลน์/Firebase ของไฟล์ที่แจกพนักงานควรเลิกใช้พร้อมกัน (นัดวันตัดกับทีม)
